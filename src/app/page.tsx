"use client";
// import styled from "styled-components";
import GlobalWrapper from "@/components/GlobalWrapper";

const Home = () => {
  return (
    <GlobalWrapper>
      a
    </GlobalWrapper>
  );
}

export default Home

