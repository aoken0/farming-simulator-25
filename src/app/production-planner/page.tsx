"use client";
import ContentTitle from "@/components/ContentTitle";
import GlobalWrapper from "@/components/GlobalWrapper"

const ProductionPlanner = () => {
  return (
    <GlobalWrapper>
      <ContentTitle>生産計画ツール</ContentTitle>
      準備中...
    </GlobalWrapper>
  )
}

export default ProductionPlanner